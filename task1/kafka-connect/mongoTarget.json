{
  "name": "mongo target",
  "config": {
    "connector.class": "com.mongodb.kafka.connect.MongoSinkConnector",
    "database": "test",
    "topics": "transfer",
    "connection.uri": "mongodb://mongo-target",
    "collection": "users"
}
}
